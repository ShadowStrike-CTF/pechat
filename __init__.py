"""
pechat — namespace placeholder.

This package exists solely to reserve the PyPI namespace.
It is not installable or usable.

Active development package: pechat-verify
    pip install pechat-verify
"""

raise ImportError(
    "pechat is a namespace placeholder and cannot be imported. "
    "Install the active package instead: pip install pechat-verify"
)
